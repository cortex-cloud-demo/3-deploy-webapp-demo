apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Reference the deployment manifest
resources:
  - deployment.yaml
# Dynamically set the Docker image name and tag
images:
  - name: IMAGE_PLACEHOLDER # Matches placeholder in deployment.yaml
    newName: "695912022813.dkr.ecr.us-east-1.amazonaws.com/monster-chrisley75-ecr-6uv" # Can be changed dynamically
    newTag: "build-4" # Can be updated via CI/CD
