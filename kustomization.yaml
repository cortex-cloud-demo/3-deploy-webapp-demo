apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Reference the deployment manifest
resources:
  - deployment.yaml
# Dynamically set the Docker image name and tag
images:
  - name: IMAGE_PLACEHOLDER # Matches placeholder in deployment.yaml
    newName: "985869370198.dkr.ecr.us-east-1.amazonaws.com/k1s-chrisley75-ecr-mri" # Can be changed dynamically
    newTag: "build-14" # Can be updated via CI/CD
