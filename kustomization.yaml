apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Reference the deployment manifest
resources:
  - deployment.yaml
# Dynamically set the Docker image name and tag
images:
  - name: IMAGE_PLACEHOLDER # Matches placeholder in deployment.yaml
    newName: "985869370198.dkr.ecr.eu-west-3.amazonaws.com/k8slab-chrisley75-ecr-4fz" # Can be changed dynamically
    newTag: "build-13" # Can be updated via CI/CD
